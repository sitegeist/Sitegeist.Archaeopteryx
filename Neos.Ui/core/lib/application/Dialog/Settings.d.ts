import * as React from 'react';
interface Props {
    initialValue?: {
        anchor?: string;
        title?: string;
        targetBlank?: boolean;
        relNoFollow?: boolean;
    };
}
export declare const Settings: React.FC<Props>;
export {};
